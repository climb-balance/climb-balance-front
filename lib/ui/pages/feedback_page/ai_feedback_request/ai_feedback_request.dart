import 'package:flutter/material.dart';

class AiFeedbackRequest extends StatelessWidget {
  const AiFeedbackRequest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
